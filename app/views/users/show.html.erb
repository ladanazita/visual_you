<h1> Welcome <%= current_user.name %> </h1>

<h2> <%= link_to 'Create new', new_post_path %> Board </h2>
<h2> <%= link_to 'Edit', edit_post_path %> Board </h2>

<%= @posts.each do |p| %>
<div>
  <%= p.title %>
  <%= image_tag p.picture.url %>
</div>
<% end %>

<%= link_to 'Take a look at other boards', controller: 'posts' %>
